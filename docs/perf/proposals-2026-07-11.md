# Performans + deneyim önerileri — 2026-07-11

5 boyutlu çok-ajanlı tarama (backend-hot / frontend-perceived / bundle-ttfi / ch-ingest / ux-datadog-bar).
29 öneri tarandı, 6 tanesi doğrulamada elendi (zaten basılmış / uygulanamaz), 23 onaylı.
Her öneri bağımsız bir ajan tarafından 'novel mi + feasible mı' diye çürütme testinden geçti; canlı ölçümler lokal v0.8.500 üzerinde.

## 1. [HIGH / ~2saat] Trace-op anomaly detektörünü raw spans çift taramasından operation_summary_5m MV'ye taşı
**Boyut:** backend-hot · **Alan:** internal/anomaly (60s tick + /api/anomalies/trace-ops)
**Kanıt:** internal/anomaly/trace_ops.go:67-103 — 'WITH cur AS (... FROM spans ...) base AS (... FROM spans ...)' 5dk cur + 1-24h base GROUP BY (service_name,name); internal/anomaly/recorder.go:154 her 60s'de koşuyor; internal/api/api.go:9392 aynı sorgu 60s TTL ile uçta. Ölçüm (system.query_log): 19.337s ve 10.346s, 690K read_rows/koşu — lokalde bile dakikada bir 10-19s'lik tarama; prod 1B span/gün'de base penceresi ~40M+ satır/dk.
**Değişiklik:** cur ve base agregalarını operation_summary_5m'in error_count_state'inden oku (MV-first invariant: 'raw spans for an aggregate = bug'). Sample trace_id MV'de yok: kalifiye olan ≤50 (service,op) çifti için İKİNCİ, service_name+name PK-prefix'li dar bir spans sorgusuyla örnek trace çek. Regresyon testi: mevcut eşik mantığı (new_error ≥3, spike ratio ≥2) tablo-testli pure fonksiyona ayrılır.
**Doğrulayıcı:** Kanıt satırları birebir doğru (trace_ops.go:67-103 çift raw-spans taraması, recorder 60s, api.go:9392 60s TTL) ve canlı query_log ~700K read_rows/koşu, 0.26-8.9s süreler gösteriyor; operation_summary_5m'de error_count_state mevcut ve MV-first invariantı bu değişikliği zaten emrediyor — git geçmişinde bu detektöre dokunan MV migrasyonu yok, hafıza yasaklarına çarpmıyor. Tek küçük düzeltme: spans PK'sı (service_name, time) — name PK'da değil set skip-index; örnek-trace sorgusu yine ucuz ama 'PK-prefix' ifadesi kısmen yanlış, ayrıca MV okuması tam 5m bucket hizası ister (v0.8.315/316 emsali, ~5dk tespit gecikmesi kabul edilmiş desen).
**Rafine plan:** Tek release (v0.8.X): trace_ops.go'yu operation_summary_5m okumasına çevir (countIfMerge, tam-bucket hizası, cluster-aware tablo adı) + kalifiye ≤50 çift için service_name+time-budaklı ikinci dar spans sorgusuyla sample trace_id/last_seen + eşik mantığını pure fonksiyona ayırıp tablo-testli trace_ops_test.go. Şema değişikliği yok, frontend değişikliği yok; ~2-3 dosya.

## 2. [HIGH / ~2saat] Anomaly detektörün servis×metrik N+1 MV okumalarını metrik başına tek batch sorguya indir
**Boyut:** backend-hot · **Alan:** internal/anomaly (2dk tick)
**Kanıt:** internal/anomaly/anomaly.go:254-257 — for services × trackedMetrics(3) → checkOne; anomaly.go:412-421 fetchBuckets her (servis,metrik) için ayrı 'FROM service_summary_5m WHERE service_name = ?' (+ seasonal ikinci okuma). Ölçüm (query_log): bu sorgular tanesi 3.4-4.5s, 50-66K read_rows — her per-servis sorgu zaten pencerenin TÜM satırlarını okuyor. Prod 1400+ servis × 3 metrik × 2 okuma ≈ 8400 sorgu/2dk.
**Değişiklik:** fetchBuckets/fetchSeasonalBaseline'ı metrik başına tek 'GROUP BY service_name, t' sorgusuna çevir (WHERE service_name IN kaldırılır, tüm servisler tek geçişte); sonuç map[service][]bucket olarak checkOne'lara dağıtılır. Evaluator emsali: v0.8.352'de aynı desenle 175× batch'lendi. Aynı satırlar BİR kez okunur; CH üstündeki sürekli arka plan baskısı (API cold p99'u şişiren ana yük) kalkar.
**Doğrulayıcı:** Kanıt satırları birebir doğru (anomaly.go:253-257 döngü, 411-420 per-servis fetchBuckets, 515-525 per-servis seasonal) ve canlı query_log doğruladı: son 1 saatte 6365 fetchBuckets + 6366 seasonal sorgusu, tanesi ort. 46-65K read_rows (ORDER BY service_name'e rağmen granule/part yuvarlaması), toplam ~708M satır/saat aynı pencereyi tekrar tekrar okuyor — metrik başına tek GROUP BY service_name,t sorgusu bunu ~6 sorgu/tick'e indirir (evaluator v0.8.352 emsaliyle aynı desen, ~70× sorgu ve ~50×+ read_rows azalması). Git log -200 ve internal/anomaly grep'inde scan yolunda hiç batch yok (batch yalnız explainer/rootcause/log_patterns'ta), hafıza yasaklarına çarpmıyor; seasonal sorgunun slot/dayClass/radius bind'leri sweep-sabiti olduğundan o da batchlenebilir.
**Rafine plan:** Tek dosya ağırlıklı (internal/anomaly/anomaly.go): scan() önce metrik başına 2 batch sorgu (fetchAllBuckets + fetchAllSeasonal, LIMIT n BY service_name + max_execution_time yükselt) → map[service][]float64 → mevcut checkOne'lara dağıt; SQL-shape table-driven test (seasonal_sql_test deseni) + /clickhouse-schema oku. ~yarım gün, tek v0.8.X release.

## 3. [HIGH / ~1saat] Evaluator + anomaly worker'ların GetServices(24h) raw spans taramasını MV'ye çevir
**Boyut:** backend-hot · **Alan:** internal/evaluator + internal/anomaly + internal/chstore
**Kanıt:** internal/evaluator/evaluator.go:312 (1dk tick) ve internal/anomaly/anomaly.go:242 (2dk tick) → GetServices(ctx, 24h, ...) → internal/chstore/repo.go:826-838 raw 'FROM spans ... GROUP BY service_name' 24h penceresi, apdex+quantile dahil. Ölçüm (query_log): 3.72M read_rows/koşu lokalde; prod 1B+ span/gün'de tam-gün taraması. İki çağıran da yalnızca servis İSİM listesini kullanıyor — aggregate kolonları çöpe gidiyor.
**Değişiklik:** Worker'lar için 'SELECT DISTINCT service_name FROM service_summary_5m WHERE time_bucket >= now()-24h' şeklinde ucuz bir ListServiceNames(ctx, window) ekle (veya GetServicesAggFilteredIn'i kullan, summary.go:386 zaten var) ve iki çağrıyı buna geçir. slo_autocreate.go:78'deki 7g çağrısı da aynı yardımcının adayı.
**Doğrulayıcı:** Kanıt satır satır doğrulandı: evaluator.go:312 (1dk tick) ve anomaly.go:242 (2dk tick) GetServices(24h) → repo.go:826-838 raw spans GROUP BY (apdex+quantile dahil) çağırıyor ve ikisi de yalnızca .Name kullanıyor; son 200 commit'te (v0.8.502'ye kadar) bu okuma MV'ye çevrilmemiş (v0.8.454 sadece pencere/timeout ekledi, v0.8.352 measure sorgularını batch'ledi), hafıza yasaklarına çarpmıyor — aksine 'raw spans for an aggregate = bug' invariantının doğrudan ihlali. Not: slo_autocreate.go:78 SpanCount/ErrorRate tükettiği için isim-listesi yardımcısına değil, mevcut GetServicesAggFilteredIn'e (summary.go:386) geçmeli; oradaki 'MV kullanıyor — cheap' yorumu bugün yanlış.
**Rafine plan:** Küçük (tek release, ~1-2 saat): (1) chstore'a ListServiceNames(ctx, window) ekle — SELECT DISTINCT service_name FROM service_summary_5m WHERE time_bucket >= now()-window (distributed-safe, /clickhouse-schema skill'e bak); (2) evaluator.go:312 ve anomaly.go:242'yi buna geçir; (3) slo_autocreate.go:78'i GetServicesAggFilteredIn'e geçir (aggregate alanları kullanıyor) ve yanlış yorumu düzelt; (4) table-driven test + go build/test gate.

## 4. [HIGH / ~10dk] Endpoints: keepPreviousData yok — her sort/range/limit değişimi tabloyu skelete düşürüyor
**Boyut:** frontend-perceived · **Alan:** frontend/perceived-speed (Endpoints)
**Kanıt:** frontend/src/lib/queries/endpoints.ts:13-23 (useEndpoints'te placeholderData yok; queryKey params'ın tamamı) + frontend/src/pages/Endpoints.tsx:252-254 (isPending → rows=undefined) ve :354 ({rows === undefined && <TableSkeleton/>}). serverSort açık (Endpoints.tsx:229), yani HER kolon başlığı tıklaması sort/dir'i değiştirip key'i yeniliyor → dolu tablo + KPI satırı skelete dönüyor.
**Değişiklik:** useEndpoints'e `placeholderData: keepPreviousData` ekle (databases.ts:41 ve Messaging.tsx:45'teki mevcut desen). İstenirse `rowsQ.isFetching && !rowsQ.isPending` iken tabloyu hafifçe soluklaştır — Services/Traces v477-479 keep-data davranışıyla birebir hizalanır.
**Doğrulayıcı:** Kanıt doğru: useEndpoints'te (endpoints.ts:13-23) placeholderData yok, serverSort açık (Endpoints.tsx:229) ve isPending→undefined→TableSkeleton zinciri (252-254, 354) her sort/range/limit değişiminde dolu tabloyu+KPI satırını skelete düşürüyor; git log -200'de Endpoints için keep-data basılmamış (v478/479/485 sadece Traces/Services/Problems). Tek düzeltme: örnek desen databases.ts:41'de değil, logs.ts:31 ve Messaging.tsx:45'te.
**Rafine plan:** Küçük: endpoints.ts'e keepPreviousData import + useEndpoints'e placeholderData satırı; opsiyonel Endpoints.tsx'te rowsQ.isFetching && !rowsQ.isPending iken tabloya hafif opacity (Traces v478 deseni). ~20dk, tek v0.8.X release.

## 5. [HIGH / ~1saat] uPlot chart'ları kararsız callback kimlikleri yüzünden her render'da yıkılıp yeniden kuruluyor
**Boyut:** frontend-perceived · **Alan:** frontend/uPlot (Dashboard, Service, Explore, Traces)
**Kanıt:** MultiLineChart.tsx:731 — rebuild-effect deps'inde `onZoom` kimliği var; oysa aynı satırda `!!onBucketClick` için presence-only + ref deseni zaten kurulmuş (725-730 yorumu). Çağıranlar inline arrow geçiyor: Dashboard.tsx:337 (onZoom → tüm panellere iner), Service.tsx:484, Explore.tsx:688. Aynı hata TimeChart.tsx:200'de: deps'te `onBrush, fmtLeft, fmtRight, fmtX` var ama onBrushRef zaten mevcut (TimeChart.tsx:55); VolumeChart.tsx:70 inline `fmtRight={(v) => v.toFixed(0)}` + Traces.tsx:567 useCallback'siz applyBrush → Traces'ta HER sayfa render'ı (30s poll, j/k seçim, satır expand) volume chart'ı destroy+new uPlot yapıyor.
**Değişiklik:** onZoom/onBrush/fmt* callback'lerini onBucketClick deseniyle ref'e al, deps'te kimlik yerine `!!cb` (presence) izle. Dashboard'da tek satır: onZoom'u useCallback'e sar. Sonuç: state değişimlerinde canvas flicker'ı, imleç/zoom kaybı ve boşa axis hesabı biter.
**Doğrulayıcı:** Kanıtların hepsi doğrulandı: MultiLineChart.tsx:731 deps'inde onZoom kimliği duruyor (oysa 725-730'da onBucketClick için presence+ref deseni zaten kurulu), TimeChart.tsx:200'de onBrush/fmtLeft/fmtRight/fmtX kimlikleri var ama onBrushRef (:55) zaten mevcut ve :159'da kullanılıyor; VolumeChart.tsx:70 inline fmtRight + Traces.tsx:567 useCallback'siz applyBrush her render'da (30s poll, j/k, expand) volume chart'ı destroy+new uPlot yaptırıyor. git log -200'de bu alana dokunan hiçbir commit yok (v402 axis, v477-485 chunk/prefetch işleriydi); canvas-swap reddi TopologyFlowGraph'a aitti, uPlot-only kuralı korunuyor.
**Rafine plan:** S — tek release: MultiLineChart'ta onZoom → ref + !!onZoom deps; TimeChart'ta onBrush/fmt* → ref + !!onBrush deps; Dashboard.tsx:337 onZoom'u useCallback'e (stale range kapanmasına dikkat), Traces.tsx:567 applyBrush'ı useCallback'e; Service/Explore inline arrow'ları primitive fix'i sonrası zararsızlaşır ama yine de sarmak temiz. tsc + görsel doğrulama (zoom/brush davranışı, presence toggle rebuild'i).

## 6. [HIGH / ~30dk] Auth şelalesini kır: me() + route chunk prefetch'i module scope'ta paralel başlat
**Boyut:** bundle-ttfi · **Alan:** TTFI / kritik yol
**Kanıt:** AppShell.tsx:115-123 loading sürerken PageLoader döndürüyor → Outlet render edilmediği için lazy route import'u /api/auth/me dönene kadar HİÇ başlamıyor; AuthProvider.tsx:44-51 me()'yi ancak ilk render sonrası useEffect'te atıyor. Ölçüm: /api/auth/me 27ms–678ms (5 çağrı: 440/27/21/678/57ms). /services soğuk açılışında Services 13.9 kB + Topbar/DataTable/Sparkline/charts (~50+ kB gz) chunk'ları bu serinin ARKASINDA iniyor. HomeRedirect'in prefetch'i de (App.tsx:88-91) Outlet içinde olduğundan auth bitmeden çalışmıyor.
**Değişiklik:** main.tsx module scope'unda (1) `export const initialMe = api.me()` başlat, AuthProvider bu promise'i tüketsin; (2) `prefetchRoute(location.pathname)` çağır ('/' → '/services' eşlemesiyle, routePrefetch.ts zaten mevcut). Route chunk indirimi ve auth doğrulaması paralelleşir; en kötü ölçümde ~680ms'lik seri aşama şelaleden düşer.
**Doğrulayıcı:** Kanıtlar birebir doğru: AppShell.tsx:115-123 loading'de PageLoader döndürüp Outlet'i (dolayısıyla lazy chunk fetch'ini) blokluyor, AuthProvider.tsx:44-51 me()'yi ancak mount-effect'te atıyor (tek çağrı yeri), HomeRedirect prefetch'i (App.tsx:88-91) Outlet içinde olduğundan auth'tan önce çalışmıyor; routePrefetch.ts mevcut ve warmed-guard'lı, api.ts+routePrefetch zaten entry chunk'ta olduğundan module-scope kullanım bundle'a yük eklemez, 401-handler yarışı da güvenli (onUnauthorized?. null-safe → catch → mevcut /login akışı). Git geçmişinde bu desen yok (v0.8.477 prefetch'i Outlet içine koydu, module scope'a değil) ve hiçbir hafıza yasağına çarpmıyor.
**Rafine plan:** Küçük (~30-40 satır): main.tsx'e `export const initialMe = api.me().catch(...)` + `prefetchRoute(location.pathname === '/' ? '/services' : location.pathname)`; AuthProvider effect'i initialMe'yi tüketecek şekilde değişir. Not: canlı warm ölçüm 13-55ms — ~680ms iddiası soğuk/kuyruk senaryosu; asıl garanti kazanım route chunk indiriminin (~50+ kB gz) auth ile paralelleşmesi. tsc + mevcut boot akışının (login'siz cold load → /login redirect) elle doğrulaması şart.

## 7. [HIGH / ~30dk] s.me handler'ında GetUserByID'ye kısa TTL cache — her cold tab'ın seri kapısı
**Boyut:** bundle-ttfi · **Alan:** backend / /api/auth/me
**Kanıt:** internal/api/api.go:5277 — s.me her çağrıda s.store.GetUserByID (CH users tablosu, FINAL okuma) yapıyor, cache yok. Ölçüm: aynı token ile ardışık çağrılar 440ms ve 678ms'e sıçrıyor (warm 21-57ms). Bu endpoint her sayfa açılışının TTFI kritik yolunda ve <200ms warm bütçesini spike'larda 3x aşıyor.
**Değişiklik:** userID→payload için 30-60s TTL'li in-memory cache (küçük map + mutex); admin user-write path'lerinde (UpsertUser vb.) invalidate. JWT claim zaten base role taşıyor, customRole gecikmesi 60s kabul edilebilir. Warm p99 <5ms'e iner, spike'lar kaybolur.
**Doğrulayıcı:** Kanıt doğru: api.go:5277'de s.me her çağrıda cache'siz GetUserByID yapıyor ve bu users FINAL CH okuması (user.go:78); AuthProvider.tsx:46 SPA'yı api.me() üzerinden gate'lediği için gerçekten her cold tab'ın seri kapısı. Son 400 commit'te ve kodda hiçbir /me cache'i yok, hafıza yasaklarına çarpmıyor; tüm user-write path'leri aynı pakette (~9 invalidation noktası) olduğundan 30-60s TTL'li map+mutex uygulanabilir ve warm çağrıları CH'den tamamen koparır — tek nüans: canlı ölçümümde (8 çağrı, 9-57ms) 440/678ms spike o an tekrarlamadı (yük/merge bağımlı) ve multi-pod'da per-pod cache TTL kadar staleness taşır (öneri 60s'i zaten kabul ediyor).
**Rafine plan:** Küçük: internal/api'de userID→payload map+RWMutex+TTL (30-60s) + 9 write-path invalidasyonu + tablo-driven TTL/invalidasyon testi; ~1-2 saat, tek release (v0.8.50x).

## 8. [HIGH / ~30dk] Evaluator/anomaly servis listesi: raw spans 24h taraması yerine MV'den isim listesi
**Boyut:** ch-ingest · **Alan:** internal/anomaly + internal/evaluator + internal/chstore
**Kanıt:** anomaly.go:242 ve evaluator.go:312 → GetServices (repo.go:237) → GetServicesFilteredIn RAW spans 24h GROUP BY (quantile(0.99)+apdex+spill settings, LIMIT 5000). query_log 2h: 128 çalıştırma × ~3.9M satır = 502M satır okuma — kutudaki en büyük tarama. İki çağıran da SADECE svc.Name kullanıyor; prod'da her tick ~1B satır tarıyor. 'Raw spans for an aggregate = bug' invariant ihlali.
**Değişiklik:** chstore'a ListActiveServiceNames(ctx, window) ekle: SELECT DISTINCT service_name FROM service_summary_5m WHERE time_bucket >= ? — ORDER BY (service_name, time_bucket) sayesinde read_in_order ile neredeyse bedava. anomaly.go:242 ve evaluator.go:312 çağrılarını buna çevir.
**Doğrulayıcı:** Kanıt birebir doğru: anomaly.go:242 ve evaluator.go:312 → GetServices (repo.go:237) → GetServicesFilteredIn (repo.go:814) raw spans 24h GROUP BY (quantile+apdex+spill), iki çağıran da sadece Name kullanıyor; service_summary_5m ORDER BY (service_name, time_bucket) + günlük partition sayesinde windowed DISTINCT gerçekten ucuz. Git log -200'de bu geçiş yok, hafıza yasaklarına çarpmıyor; tek nüans summary.go'da zaten penceresiz ListServiceNames + v0.8.234 raw-fallback deseni var — yeni helper onu şablon almalı.
**Rafine plan:** S (tek release): chstore/summary.go'ya ListActiveServiceNames(ctx, window) ekle — ListServiceNames'in windowed varyantı (WHERE time_bucket >= ?, SETTINGS max_execution_time), v0.8.234'teki boş-MV raw-spans fallback'i ŞART (degraded ALLOW_UNSET_CLUSTER prod'da summary MV'ler bilerek boş); anomaly.go:242 + evaluator.go:312 çağrılarını çevir; mv_routing_test.go tarzı SQL-shape testi + go test ./...

## 9. [HIGH / ~1saat] FindOpenProblem nokta sorguları → tick başına tek FINAL snapshot
**Boyut:** ch-ingest · **Alan:** internal/evaluator + internal/anomaly + internal/chstore/problem.go
**Kanıt:** problem.go:1102-1110 (FROM problems FINAL WHERE rule_id=? AND service=? ... LIMIT 1); çağıranlar evaluator.go:595/734/993, anomaly.go:303, db_capacity.go:229, slo_burn.go:86. query_log 2h ŞAMPİYONU: 55.878 çalıştırma, avg 30ms, toplam 1.657s CH süresi; problems ORDER BY id olduğundan her çağrı ~4.8k satırı FINAL ile yeniden okuyor (~657 (rule,service) çifti × tick).
**Değişiklik:** Tick başında TEK ListProblems(status IN open,acknowledged) → map[(rule_id,service)]Problem snapshot'ı; FindOpenProblem çağrıları map lookup olur. v0.8.352 batched-prefetch (175×) deseninin birebir devamı. ~657 sorgu/tick → 1; system_settings/monitors FINAL okumalarındaki 337ms avg'lik kuyruk gecikmesi de rahatlar.
**Doğrulayıcı:** Kanıt birebir doğru: FindOpenProblem problem.go:1099-1122'de FINAL nokta sorgusu, altı çağıran da (evaluator.go:595/734/993, anomaly.go:303, db_capacity.go:229, slo_burn.go:86) tick döngülerinde; evaluator tick'i 1 dk olduğundan 55.9k/2h sayısı ~465-657 çift/tick ile tutarlı. v0.8.352 yalnız ölçüm okumalarını batch'ledi, problem-state okumalarına hiç dokunmadı — bu öneri o desenin doğal devamı ve git log -200 + grep'te benzeri yok, hiçbir hafıza yasağına çarpmıyor. Tek ufak detay: ProblemFilter.Status tek değerli, snapshot için küçük bir Statuses eklentisi ya da ayrı metod gerekir (ListStaleOpenProblems emsal).
**Rafine plan:** S-M (~yarım-1 gün): chstore'a SnapshotOpenProblems (status IN open,acknowledged → map[(rule_id,service)] en-yeni started_at) + 4-5 tick döngüsünün başına map enjeksiyonu (evaluator/anomaly/db_capacity/slo_burn, opsiyonel monitor runner; hata halinde nokta-sorgu fallback) + table-driven regression test (v0.8.352 prefetch deseni)

## 10. [HIGH / ~2saat] Anomaly bucket fetch'leri: servis başına 6 sorgu yerine metrik başına tek GROUP BY
**Boyut:** ch-ingest · **Alan:** internal/anomaly/anomaly.go
**Kanıt:** anomaly.go:412 (fetchBuckets, 24h) + anomaly.go:517 (seasonalBaselineSQL, 14d) servis×metrik×tick başına ayrı sorgu; tick 2dk (anomaly.go:209). query_log satır 3-8: şekil başına ~3.7k çalıştırma/2h, TEK servis için 44-63k satır okuma (unmerged AggregatingMergeTree part amplifikasyonu — aynı part'lar N servis için N kez açılıyor). Prod 1400 servis → ~8.400 sorgu/tick = ~70 qps sadece anomaly.
**Değişiklik:** Tick başına metrik başına TEK sorgu: WHERE time_bucket >= ? GROUP BY service_name, time_bucket (seasonal için aynı şekil + sınıf/slot filtresi), sonuç bellekte servislere dağıtılır. 6N sorgu/tick → 6; part'lar tek geçişte taranır.
**Doğrulayıcı:** Kanıt doğru: anomaly.go:209 (2dk tick), :411-418 fetchBuckets ve :515-525 seasonalBaselineSQL servis başına ayrı sorgu atıyor; 3 metrik × 2 şekil = 6 sorgu/servis/tick, 1400 serviste ~8.400/tick. v0.8.352 aynı batching desenini SADECE evaluator'a uyguladı (internal/anomaly hiç dokunulmadı, dosya git geçmişi temiz) — yani öneri hem yeni hem de kanıtlanmış bir desenin eksik kalan ikizi; hiçbir hafıza yasağına çarpmıyor.
**Rafine plan:** S-M: internal/anomaly/anomaly.go — scan() başında tick başına 6 batched prefetch (3 metrik × consecutive/seasonal, GROUP BY service_name, time_bucket), sonuç map[service][]float64 olarak checkOne'a inject; LIMIT'ler LIMIT ... BY service_name'e uyarlanır; evaluator v0.8.352 prefetchMeasures şablonu + SQL-shape testleri (seasonalBaselineSQL zaten pure/test edilebilir).

## 11. [HIGH / ~1saat] ⌘K servis araması bugün ölü — eager fetch'i sunucu aramasına çevir
**Boyut:** ux-datadog-bar · **Alan:** keyboard / ⌘K
**Kanıt:** frontend/src/components/CommandPalette.tsx:201 — api.services({from:0,to:0}); canlı API cevabı {"services":null} (doğrulandı), SERVICES_CACHE=[] olarak oturum boyu kilitleniyor; limit paramı da yok (sunucu default 50 / 1400+ servis)
**Değişiklik:** Palette'in eager servis listesini kaldır; aynı dosyadaki endpoint aramasının deseniyle (200ms debounce, stale-guard) api.serviceNames(q, 20) çağır. Pinned/recent boş-sorgu rotasyonu aynen kalır. Picker=server-side search kuralına da uyum sağlanır.
**Doğrulayıcı:** Kanıt birebir doğru: CommandPalette.tsx:201'de api.services({from:0,to:0}) canlıda {"services":null} dönüyor (doğrulandı), api.ts:100-104 null'u geçirince SERVICES_CACHE=[] (truthy) oturum boyu kilitleniyor — ⌘K servis araması gerçekten ölü. api.serviceNames → /api/service-names canlıda çalışıyor (q=pay → 5 isim) ve aynı dosyadaki endpoint-arama deseni (satır 221-239, 200ms debounce + stale-guard) birebir kopyalanabilir; pivotSvcs rotasyonu SERVICES_CACHE'ten bağımsız, aynen kalır. 500 commit tarandı: basılmamış, hiçbir hafıza yasağına çarpmıyor; aksine "picker = server-side search" kuralını yerine getiriyor.
**Rafine plan:** Küçük (tek dosya, ~30 satır): CommandPalette.tsx'te 197-213 eager fetch + SERVICES_CACHE kaldır; endpoint-arama useEffect'inin (221-239) ikizi olarak api.serviceNames(q, 20) ile debounced servis araması ekle; results memo'sunda services artık sunucu-filtreli geldiğinden fuzzy skorlamadan çıkar (endpoints gibi ekle veya sabit skor ver). tsc gate + tek v0.8.X release.

## 12. [HIGH / ~10dk] Problem→Error traces pivotu problem penceresini taşımıyor
**Boyut:** ux-datadog-bar · **Alan:** cross-signal pivot
**Kanıt:** frontend/src/features/anomalies/ProblemDetail.tsx:184 ve :467 — logsHref range=custom:<from>-<to> taşırken tracesHref sadece ?service=…&hasError=true; /traces sticky range ile açılıyor (Traces.tsx:153 useUrlRange), eski problemde boş liste
**Değişiklik:** Her iki detail varyantında tracesHref'e logsHref ile aynı custom pencereyi ekle (&range=custom:...). Exception grubunda firstSeen−30m..lastSeen+10m, alert probleminde startedAt−1h..end+10m zaten hesaplanıyor.
**Doğrulayıcı:** Kanıt doğru: ProblemDetail.tsx:184 ve :467'de tracesHref pencere taşımıyor, logsHref ise aynı satırlarda hesaplanan custom pencereyi taşıyor; Traces.tsx:153 useUrlRange + urlState decodeRange 'custom:from-to' formatını URL önceliğiyle tüketiyor, yani eklenti iddia edilen etkiyi verir. Git log (300 commit) tarandı — v0.8.484 yalnız Logs linkini düzeltmiş, traces pivotu hiç ele alınmamış; hafıza yasaklarıyla çakışma yok.
**Rafine plan:** XS — tek dosyada 2 satır: her iki varyantta tracesHref'e mevcut logsFrom/logsTo ile &range=custom:<from>-<to> ekle (exception grubunda ayrıca mapHref değil yalnız traces); tsc + tek v0.8.X release, backend değişikliği yok.

## 13. [MEDIUM / ~1saat] listProblems'ın 5 seri zenginleştirme round-trip'ini errgroup ile paralelle
**Boyut:** backend-hot · **Alan:** internal/api (/api/problems)
**Kanıt:** internal/api/api.go:8752-8832 — ListProblems(FINAL) → EnrichProblemsWithRunbooks → Teams → Clusters → Deploys → RootCause tamamı SERİ. Ölçüm: ilk hit 14.0s, refresh=1 ile 6.1s (CH soğuk) / 0.6-0.8s (CH sıcak); bütçe hot uç <50ms warm, <1s cold. Altta yatan fetch'ler birbirinden bağımsız: alert-rules cache (problem.go:651), servis kataloğu (problem.go:695), cluster map (repo.go:353, 60s cache), deploys map (problem.go:360, 15s cache), hypotheses (rootcause_hypothesis.go:304).
**Değişiklik:** ListProblems döndükten sonra 5 zenginleştirmenin VERİ fetch'lerini errgroup ile eşzamanlı çek (cluster map + kataloğ + rules ListProblems ile de paralel başlayabilir — probleme bağımlı değiller), uygulamayı sırayla yap. Inbox emsali: v0.8.472 errgroup count. Cold path ~toplam yerine ~en uzun bacağa iner.
**Doğrulayıcı:** Kanıt satır satır doğrulandı (api.go:8752-8832 seri zincir; problem.go:651/694/360, repo.go:354, rootcause_hypothesis.go:304 mevcut) ve canlıda repro edildi: ilk hit 26.0s soğuk / 77ms cache — <1s cold bütçesinin çok üstünde. git log -200'de örtüşen iş yok: v0.8.359 fetch'leri TTL-cache'ledi ama zinciri seri bıraktı, v0.8.472 errgroup emsali farklı uçta (inbox count); yasaklara çarpmıyor (x/sync zaten bağımlılık). TTL cache'ler sayesinde "paralel prefetch + seri apply" deseni semantiği bozmadan uygulanabilir.
**Rafine plan:** Küçük-orta (~1 saat, tek dosya ağırlıklı): api.go listProblems içinde errgroup — (1) ListProblems ile eşzamanlı: ListAlertRules + ListServiceMetadata + GetServiceClusterMap prefetch (üçü de v0.8.359 TTL-cache'li, warm-up çağrısı yeter); (2) problemler dönünce fetchDeploysByService'i tetikleyen Deploys enricher çalışır; (3) apply sırası aynen seri kalır (Priority→Deploys sonrası, team filter→Teams sonrası). RootCause'u post-filter'da bırak (IN-list küçük kalsın) ya da tam-set IN'e razı ol — karar notu commit'e. İnbox v0.8.472 deseni + soft-fail semantiği korunmalı; regression: cold-path timing ölçümü + go test ./...

## 14. [MEDIUM / ~30dk] getServices recompute'unda 3 seri CH bacağını errgroup ile paralelle
**Boyut:** backend-hot · **Alan:** internal/api (/api/services)
**Kanıt:** internal/api/api.go:1474 (GetServicesAggFilteredIn) → api.go:1489 (GetOpenProblemCountsByService, problems FINAL) → api.go:1507 (CountServicesAgg, withTotal) tamamı seri. Ölçüm: refresh=1 ile 1.10-1.15s SABİT (iki koşu), warm HIT 23ms; hot uç bütçesi <50ms warm — 30s TTL'de her SWR yenilemesi ve her yeni (sayfa,filtre,bucket) anahtarı bu 1.1s'yi ödüyor.
**Değişiklik:** Üç bacağı errgroup'a al: MV liste sorgusu, açık-problem sayaçları ve (withTotal ise) CountServicesAgg eşzamanlı koşsun; sayaç birleştirme wg.Wait() sonrası. Sayaç sorgusu ayrıca #5'teki FINAL setting'inden de kazanır. Recompute ~en uzun tek sorguya iner (~yarıya).
**Doğrulayıcı:** Üç CH bacağı (api.go:1474 liste, 1489 problem sayaçları, 1507 CountServicesAgg) gerçekten seri ve birbirinden bağımsız; birleştirme Wait sonrası yapılabilir. Emsal desen inbox.go:286'da (v0.8.472) zaten var, getServices'e uygulanmamış — git log -200'de basılmış izi yok, hafıza yasaklarına çarpmıyor.
**Rafine plan:** S — tek fonksiyon (getServices recompute closure) içinde errgroup/WaitGroup; counts+total soft-fail semantiği korunacak (sadece liste sorgusu fatal), sonra refresh=1 ile önce/sonra ölçüm.

## 15. [MEDIUM / ~2saat] Poll yenilemesinde uPlot'a setData fast-path yok — 30s'de bir full re-create
**Boyut:** frontend-perceived · **Alan:** frontend/uPlot (tüm zaman-serisi panelleri)
**Kanıt:** TimeChart.tsx:189-190 (`plotRef.current?.destroy(); plotRef.current = new uPlot(...)`, deps'te times/series — TimeChart.tsx:200), MultiLineChart.tsx:707 (deps'te series — :731), pages/service/charts/OverviewChart.tsx:173-174, components/viz/TimeSeriesPanel.tsx:613. Her 30s poll yeni dizi kimliği üretiyor → seri sayısı/etiketleri değişmese bile chart sıfırdan kuruluyor (axes, legend, listener'lar); keep-data v479 veriyi tutsa da grafik yine 'göz kırpıyor'.
**Değişiklik:** Rebuild effect'ini ikiye böl: seri anahtarları/opsiyon-etkileyen prop'lar değişince re-create, yalnız veri değişince `u.setData(data)` (MultiLineChart'taki setSeries click-to-isolate yorumu zaten <1ms yolunu belgeliyor, :737+). Önce TimeChart + MultiLineChart'a uygula; Overview/TimeSeriesPanel aynı deseni kopyalar.
**Doğrulayıcı:** Kanıt satırları doğrulandı (TimeChart.tsx:189-200, MultiLineChart.tsx:707/731, OverviewChart.tsx:173-174, TimeSeriesPanel.tsx:613); kod tabanında hiç u.setData çağrısı yok ve son 300 commit'te bu iş basılmamış — MultiLineChart'ın kendi yorumu (:32-38, :733-736) rebuild'in ~30ms, setSeries yolunun <1ms olduğunu zaten belgeliyor, yani fast-path etkisi gerçekçi. Tek dikkat: tooltip hook'ları times/series'i closure'dan okuyor, data-only yolda ref'e taşınmalı (onBrushRef/bucketClickRef deseni zaten mevcut); ayrıca setData yolu her poll'da sıfırlanan click-to-isolate durumunu da korur.
**Rafine plan:** Orta (S-M): önce TimeChart + MultiLineChart — effect'i ikiye böl (seri imzası/opsiyon prop'ları → re-create; yalnız veri → u.setData) + tooltip closure'larını ref'e al; sonra aynı desen OverviewChart + TimeSeriesPanel'e kopyalanır. ~4 dosya, 1 gün, her adım ayrı v0.8.X.

## 16. [MEDIUM / ~10dk] prefetchRoute haritasında triage/alerting rotaları eksik — Problems/Anomalies/Alerts'e ilk tıklama chunk RTT'si ödüyor
**Boyut:** frontend-perceived · **Alan:** frontend/route-chunks (routePrefetch)
**Kanıt:** lib/perf/routePrefetch.ts:16-36 — importers'ta '/problems', '/anomalies', '/alerts', '/slos', '/events', '/runbooks', '/system' yok; hepsi Sidebar.tsx:59-116'da kalıcı link (özellikle /problems triage badge'iyle en sıcak hedef, chunk'ı da ayrı: App.tsx:46 features/anomalies). Hover-prefetch listener'ı (App.tsx:96) bu rotalarda no-op kalıyor. Ayrıca ':34 /profiling girdisi bayat — v0.8.489'da sidebar'dan gizlendi.
**Değişiklik:** importers'a yedi rotayı ekle (lazy() ile birebir aynı import specifier'ları: '/problems' → import('@/pages/../features/anomalies') vb.), '/profiling' satırını kaldır. Sidebar hover'ı chunk'ı idle'da ısıtır; ilk tıklamada Suspense flaşı kalmaz.
**Doğrulayıcı:** Kanıt birebir doğru: routePrefetch.ts:15-36 importers'ta yedi triage/alerting rotası yok, hepsi Sidebar'da kalıcı anchor ve hover listener (App.tsx:96) onlarda no-op; dosya v0.8.224'ten beri hiç dokunulmamış, v477-500 dalgası bu haritaya girmemiş. Tek kritik detay öneride zaten var: '/problems' → '@/features/anomalies', '/anomalies' → '@/features/anomalies/AnomalyStreamsPage' (App.tsx:46-47, pages/ değil); '/system' → '@/pages/System' redirect'e rağmen doğru chunk. '/profiling' satırı v0.8.489 sidebar gizlemesiyle bayat, kaldırmak zararsız.
**Rafine plan:** XS — tek dosya (routePrefetch.ts), ~8 satır ekle/1 satır sil; App.tsx:46-47'deki features/anomalies specifier'larını birebir kopyala, tsc + vite build yeterli

## 17. [MEDIUM / ~10dk] manualChunks '@tanstack' kuralını daralt: virtual-core eager yüklenmesin
**Boyut:** bundle-ttfi · **Alan:** bundle / vite.config.ts
**Kanıt:** vite.config.ts:53 `id.includes('@tanstack')` → tanstack chunk'ı (68.3 kB min / 20.4 kB gz, index.html'de modulepreload) sourcemap atfına göre 21.3 kB'ı @tanstack/virtual-core+react-virtual — bunları yalnız lazy route'lardaki sanal tablolar kullanıyor, ilk boyada gereksiz (~6.7 kB gz).
**Değişiklik:** Kuralı `id.includes('@tanstack/query-core') || id.includes('@tanstack/react-query')` olarak daralt; virtual-core/react-virtual rollup'ın varsayılanıyla kullanan route chunk'larına taşınır. Eager gz ~112.6→~106 kB.
**Doğrulayıcı:** Kanıt doğru (vite.config.ts:53, tanstack chunk 68.3 kB min / 20.4 kB gz modulepreload'lu, sourcemap'te virtual-core+react-virtual yalnız lazy sayfalarca kullanılıyor) ve fikir daha önce basılmamış (chunk geçmişi sadece v0.8.142 dagre + v0.8.477 zone.js). Ancak öneri YAZILDIĞI HALİYLE çalışmıyor: satır 79'daki `return 'vendor'` catch-all'u react-virtual'ı eager vendor'a itiyor (deneysel build: vendor 54.3→61.4 kB gz, net eager +0.3 kB); catch-all'dan önce virtual paketlerine erken `return undefined` eklenen tek satırlık düzeltme build ile doğrulandı — eager toplam ~112.6→~106.3 kB gz, iddia edilen etkiyi birebir veriyor.
**Rafine plan:** vite.config.ts manualChunks: satır 53'ü daraltmak YETMEZ (satır 79 `return 'vendor'` catch-all'u yakalar). Doğrulanmış düzeltme: tanstack kuralından ÖNCE `if (id.includes('@tanstack/virtual-core') || id.includes('@tanstack/react-virtual')) return undefined;` ekle (mevcut `@tanstack` kuralı kalabilir). Build-verified sonuç: virtual modüller lazy paylaşımlı chunk'a (6.7 kB gz, __vite__mapDeps ile yalnız Traces/AdminSql/System vb. route'larla iner), tanstack 20.46→14.14 kB gz, vendor+entry bayt-bayt aynı; eager gz ~112.6→~106.3 kB. Efor: 1 satır + build; risk düşük (react-virtual kullanıcıları yalnız lazy sayfalar, grep ile doğrulandı).

## 18. [MEDIUM / ~1saat] CommandPalette + CopilotChat'i AppShell'den lazy-mount et
**Boyut:** bundle-ttfi · **Alan:** bundle / entry chunk
**Kanıt:** Sourcemap atfı: entry index-B-mbtQkM.js (111.6 kB min) içinde CommandPalette.tsx 11.5 kB + lib/actions.ts 3.8 kB + CopilotChat.tsx 5.5 kB = 20.8 kB min (~6.3 kB gz). AppShell.tsx:154 ve :164 her sayfada mount ediyor ama ikisi de ilk etkileşime kadar (Cmd-K / launcher tık) görünmez.
**Değişiklik:** İnce bir hotkey/launcher stub bırak; ilk Cmd-K veya launcher tıkında `lazy(() => import(...))` ile yükle + requestIdleCallback'te ısıt (browserOtel deseniyle aynı, main.tsx:48-56 emsal). Entry parse'ı ~21 kB min küçülür, davranış değişmez.
**Doğrulayıcı:** Kanıt doğrulandı: AppShell.tsx:154/:164 eager mount, entry index-B-mbtQkM.js 111.9 kB ve sourcemap ataması bire bir tuttu (CommandPalette 11.7k + CopilotChat 5.6k + actions.ts 3.9k = 21.3 kB min; actions.ts'in diğer importer'ı InboxTriageDrawer entry'de değil, yani eviction gerçek). Son 220 commit'te ve v471-500 listesinde bu iş yok, yasaklara çarpmıyor; main.tsx:48-56 idle-warm emsali mevcut.
**Rafine plan:** S (tek release): AppShell'de iki lazy() + ince stub — stub Cmd-K dinleyicisini (useShortcuts, keyboard.ts zaten entry'de) ve CopilotChat launcher butonu + copilotConfig probe'unu korumalı; requestIdleCallback'te ısıt (main.tsx:48-56 deseni). Net entry kazanımı stub sonrası ~19-20 kB min (~6 kB gz). Doğrulama: build sonrası sourcemap'te üç dosyanın entry'den çıktığını + Cmd-K ve launcher davranışının aynı kaldığını kontrol et.

## 19. [MEDIUM / ~yarım gün] Exception JSON_VALUE kazısı → insert-time ex_type/ex_msg kolonları
**Boyut:** ch-ingest · **Alan:** internal/otlp + internal/chstore/exception*.go
**Kanıt:** exception.go:31-33 (exTypeExpr/exMsgExpr her satırda events JSON'ını AYRI AYRI parse eder — satır başına 2 parse), exception_inbox.go:657+834 ve endpoints_detail.go:222 stacktrace için üçüncü JSON_VALUE; beş sorgu sitesi (GetExceptions, RefreshExceptionGroups, samples, occurrences, EndpointTopExceptions) raw spans pencere taramasında bu ifadeleri çalıştırıyor. Prod 1B span/gün × %1-5 hata = milyonlarca satır × 2-3 JSON parse.
**Değişiklik:** OTLP ingest'te exception.type/message'ı düz kolona yaz: ex_type LowCardinality(String) + ex_msg String (hasXCol probe + koşullu INSERT + skip ALTER — distributed-safe day-one kuralı, per schema-types-lowcardinality). Sorgu ifadeleri kolona düşer, JSON parse sıfırlanır; ileride exceptions fingerprint MV'sinin kapısı açılır.
**Doğrulayıcı:** Kanıt satırları birebir doğru (exception.go:31/33 satır başına 2 JSON parse; inbox 657/834 + endpoints_detail 222 stacktrace için üçüncüsü; 5 sorgu sitesi kod yorumuyla teyitli) ve spans şemasında ex_type/ex_msg yok — v494/v500 yalnız query-time düzeltmeydi, hiçbir hafıza yasağına çarpmıyor. hasOpGroupCol/hasSeriesFpCol probe + koşullu named INSERT şablonu repo.go:70-130'da hazır, ingest convert.go:117-124'te structured event erişimi var; tek dikkat noktası eski satırlar '' kalacağından sorgu sitelerinde geçiş coalesce'i (op_group v0.8.186 oyun kitabı) ve stacktrace'in kolonsuz JSON'da kalması.
**Rafine plan:** Orta (tek release): spans'a ex_type LowCardinality(String) + ex_msg String (probe + koşullu ALTER/INSERT, distributed-safe), convert.go'da exception event + error.type fallback çıkarımı, 5 sorgu sitesinde kolon-öncelikli coalesce ifadesi, regression testler. Stacktrace kolonu ve fingerprint MV ayrı faz.

## 20. [MEDIUM / ~2saat] Env/cluster katalog poll'ları: raw spans yerine katalog MV (+ çift taramayı hemen tekle)
**Boyut:** ch-ingest · **Alan:** internal/chstore/repo.go
**Kanıt:** repo.go:538-556: env picker AYNI raw-spans taramasını İKİ kez yapar (GROUP BY deploy_env + uniqExact(deploy_env)). query_log 2h: 212+209 çalıştırma (~34s'de bir, ~190k satır/çalıştırma) + cluster katalogu (res_values kazısı) 206 çalıştırma — toplam ~145M satır/2h sürekli arka plan yükü.
**Değişiklik:** Adım 1 (10dk): uniqExact ikinci taramasını kaldır — toplamı aynı GROUP BY sonucundan türet (LIMIT'e takıldıysa count() OVER ile). Adım 2: metric_catalog (v0.8.396) deseninde küçük service_env_catalog_5m MV'si (service_name, deploy_env, cluster, time_bucket) — env picker, GetServiceEnvironments ve cluster katalogu buradan okur; distributed-safe MV oluşturma şart.
**Doğrulayıcı:** Kanıt birebir doğru ve canlıda teyitli: repo.go:537-544 GROUP BY + 551-555 aynı-WHERE uniqExact çift taraması duruyor; CH query_log son 2h = 221+221 env taraması (49M+49M satır) + 217 cluster derive (48M) ≈ 146M satır/2h sürekli arka plan yükü. Novel: hiçbir env/cluster katalog MV'si yok; v0.8.389 raw taramayı korudu (uniqExact'i o ekledi), v0.8.396 metric_catalog deseni yalnız metrik adlarına uygulandı; Stage-1 env dokümanının ertelediği şey farklı (summary MV'lere env BOYUTU, Dilim 5), küçük enumeration katalog MV'si tartışılmadı/reddedilmedi; hafıza yasaklarına çarpmıyor, MV-first invariantıyla uyumlu. Tek şart (öneri zaten söylüyor): MV distributed-safe doğmalı (metric_catalog'un cluster.go highVolumeTables/sharding + boş-katalog raw-fallback emsali hazır) ve cluster derive insert anında MV'de hesaplanmalı.
**Rafine plan:** Adım 1 (uniqExact kaldır, count() OVER () ile tek tarama + regresyon testi): ~30-45dk. Adım 2 (service_env_catalog_5m MV: DDL store.go + cluster.go distributed wiring + ListEnvironments/GetServiceEnvironments/ListClusters okuyucuları + boş-katalog raw fallback + testler): ~yarım gün; /clickhouse-schema skill'i önce okunmalı.

## 21. [MEDIUM / ~30dk] Inbox triage drawer'da root cause bir tık arkasında ve yanıltıcı
**Boyut:** ux-datadog-bar · **Alan:** triage tık sayısı
**Kanıt:** frontend/src/components/InboxTriageDrawer.tsx:108 — RootCauseRibbon summary={undefined}: chip her zaman 'no clear cause yet' okunuyor; AnomaliesPage.tsx:1057 gerçek persisted summary geçiyor. Akış: satır → drawer → ▸ tık → fetch bekle → exemplar trace = 3 tık
**Değişiklik:** RootCauseRibbon'a defaultOpen prop'u ekle; drawer tek öğe için açıldığında ribbon otomatik expand olsun (drawer open = fetch-on-open, ES-cost disiplinine uygun; liste hâlâ lazy). Triage başına bir tık + yanıltıcı etiket gider; exemplar trace direkt görünür.
**Doğrulayıcı:** Kanıt doğrulandı: InboxTriageDrawer.tsx:108 gerçekten summary={undefined} geçiyor ve RootCauseRibbon.tsx:43-44 (hasCause = !!summary && conf>0.05) nedeniyle chip drawer'da her zaman "no clear cause yet" okuyor — AnomaliesPage.tsx:1057 ise gerçek p.rootCause geçiyor; akış satır→drawer→▸→fetch = 3 etkileşim iddiası doğru. Git log (v0.8.169 ribbon, v0.8.292 drawer, v0.8.498 ui/Drawer migrasyonu) hiçbir yerde auto-expand/defaultOpen içermiyor; defaultOpen deseni zaten evde var (ServiceStructure/DBQueriesPanel/ServiceNeighbors aynı prop'u kullanıyor), hiçbir hafıza yasağına çarpmıyor ve fan-out CH-tabanlı olduğundan drawer-open'da fetch ES-cost disipliniyle uyumlu.
**Rafine plan:** Küçük (~20-25 satır, 2 dosya): RootCauseRibbon'a defaultOpen?: boolean prop + useState(defaultOpen) + mount'ta fetch için useEffect (onToggle çalışmayacağı için lazy-fetch'i mount'a da bağla); InboxTriageDrawer.tsx:108'e defaultOpen geç. Not: InboxItem'da persisted RootCauseSummary yok (inboxDrawer.ts:18 sadece capability flag), yani yanıltıcı chip'i summary geçerek düzeltmek backend değişikliği isterdi — defaultOpen bunu tek frontend dokunuşuyla aşıyor; defaultOpen açıkken chip etiketini "analyzing…" gibi nötrlemek opsiyonel cila. tsc + mevcut inboxDrawer.test.ts yeterli, tek v0.8.X.

## 22. [MEDIUM / ~30dk] Env filtresi /inbox ve /problems'ta uygulanıyor ama oradan değiştirilemiyor
**Boyut:** ux-datadog-bar · **Alan:** range/env tutarlılığı
**Kanıt:** frontend/src/components/Topbar.tsx:20 — EnvPicker yalnız range prop'u varken render oluyor; Inbox.tsx:186 ve AnomaliesPage.tsx:337 Topbar'a range geçmiyor, ama sticky localStorage env sorguları filtreliyor (Inbox.tsx:106-113); tek gösterge non-interactive hint chip
**Değişiklik:** Topbar'a showEnv (veya env: true) prop'u ekle; Inbox ve Problems bunu geçsin ki EnvPicker range'siz sayfalarda da görünsün. Alternatif/ek: facet bar'daki env chip'ine ✕ (clear) koy. Dünkü 'uat' pick'i inbox'ı sessizce daraltması durumundan çıkılır.
**Doğrulayıcı:** Tüm kanıtlar doğrulandı: Topbar.tsx:20 EnvPicker'ı range'e kilitliyor; Inbox.tsx:186 ve features/anomalies/AnomaliesPage.tsx:337 (dosya pages/ değil features/anomalies/ altında) Topbar'a range geçmiyor ama sticky env sorguları filtreliyor (Inbox.tsx:106-113, AnomaliesPage.tsx:711/718); tek gösterge cursor:help'li non-interactive chip (Inbox.tsx:233-241). Git log'da v0.8.387 chip'i bilerek non-interactive bırakmış, sonrasında (v500'e kadar) düzelten commit yok; hafıza yasaklarına çarpmıyor. EnvPicker prop'suz ve self-contained olduğundan showEnv prop'u + chip'e ✕ (setEnv('') zaten URL+localStorage temizliyor) iddia edilen etkiyi verir.
**Rafine plan:** Küçük (S): Topbar'a showEnv?: boolean prop'u (range gate'ine || showEnv); Inbox.tsx:186 + AnomaliesPage.tsx'teki 4-5 Topbar çağrısına showEnv geçir; opsiyonel: hint chip'e ✕ (useUrlEnv setEnv('')). ~3 dosya, tsc gate, tek release.

## 23. [MEDIUM / ~1saat] Çakışan iki 'g x' kısayol kaydı + ⌘K sayfa kataloğu çürümüş
**Boyut:** ux-datadog-bar · **Alan:** keyboard / ⌘K
**Kanıt:** AppShell.tsx:75-92 ve GlobalShortcuts.tsx:12-24 ikisi de 'g <x>' kaydediyor; registry Map (keyboard.ts:167) son yazan kazanır → 'g m'=Metrics ve 'g o'=Topology hiç çalışmıyor; CommandPalette.tsx:39-69 kataloğu /topology,/errors,/status,/admin/* redirect'lerine işaret ediyor, Inbox/Service Map/Endpoints/Hosts girdileri hiç yok ('map' araması sonuçsuz)
**Değişiklik:** Tek kısayol listesine konsolide et (AppShell'deki bloğu sil, GlobalShortcuts'ı gerçek rotalarla güncelle; 'g n'→/inbox ekle). Palette PAGES'ı güncel rotalara çevir ve eksik sayfaları (Inbox, Service Map, Endpoints, Hosts, Events, System sekmeleri) ekle.
**Doğrulayıcı:** Kanıt birebir doğru: AppShell.tsx:75-92 ve GlobalShortcuts.tsx:12-24 aynı 'g x' prefix'lerini kaydediyor; GlobalShortcuts child olduğu için effect'i önce çalışıyor, keyboard.ts:167 Map'inde AppShell son yazan olarak kazanıyor → 'g m'=Metrics ve 'g o'=Topology gerçekten hiç tetiklenmiyor. CommandPalette PAGES'ı /topology,/errors,/status,/admin/* redirect'lerine işaret ediyor ve /inbox,/service-map,/endpoints,/hosts,/events,/system rotaları katalogda yok. Git geçmişinde (v0.4.8 + v0.5.444 ayrı basılmış, hiç konsolide edilmemiş) ve hafıza yasaklarında engel yok.
**Rafine plan:** Küçük (~1-2 saat, tek release): (1) AppShell.tsx'teki useShortcuts bloğunu sil ama SADECE silme değil birleştirme — 'g r'(Runbooks), 'g x'(Alerts), 'g c'(System stats→/system/stats) yalnız AppShell'de, 'g h'(Home) yalnız GlobalShortcuts'ta; hepsi tek PAGES listesinde toplanmalı. (2) GlobalShortcuts PAGES: 'g m'→/service-map mi /metrics mi kararlaştır (Datadog kası: m=map), 'g n'→/inbox ekle, 'g o' çakışmasını çöz. (3) CommandPalette PAGES: Topology→Service Map(/service-map), Errors girdisini kaldır (Problems zaten var), Status→System(/system/stats), Admin·*→/system/:tab rotalarına; Inbox/Endpoints/Events ekle. Sidebar-gizli sayfalar (Hosts/Profiling/Monitors/External) için tek politika kararı: palette'te escape-hatch olarak kalsın ya da hepsi çıksın. Sadece frontend, tsc gate yeter; '?' yardım modalı registry'den okuduğu için otomatik düzelir.
