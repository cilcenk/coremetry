{
  "schema": 1,
  "baseUrl": "http://localhost:8090",
  "runs": 5,
  "warmup": 1,
  "rules": { "tolerancePct": 25, "datasetDriftWarnPct": 20 },
  "_doc": "docs/perf/perf-budget-2026-08-28.md §3 — nokta adı = self-telemetry span adı (spanmetrics_1m.name). Eşikler = perfcheck DÜZELTİLMİŞ taban koşusu (2026-08-28, minikube, 24h 1.46M span, 100 servis, her soğuk koşu kaydırılmış pencere) medyanının ≈1.5×ı; arzu = darboğaz giderilince. Soğuk = refresh=1 (X-Cache BYPASS). Karar: medyan; fail = bütçe aşımı VE önceki koşuya göre +tolerans.",
  "points": [
    {
      "name": "GET /api/traces", "scenario": "mv-24h", "window": "24h", "cold": true,
      "path": "/api/traces?limit=50&offset=0&count=skip&from={from}&to={to}",
      "budget": { "achievableMs": 600, "aspirationalMs": 300 },
      "note": "P1 — MV iki aşama, pencereden bağımsız; perfcheck taban p50 334 ms (p95 1.1 s)"
    },
    {
      "name": "GET /api/traces", "scenario": "raw-attr-filter-24h", "window": "24h", "cold": true,
      "path": "/api/traces?limit=50&offset=0&count=skip&from={from}&to={to}&filters=%5B%7B%22k%22%3A%22user_agent.original%22%2C%22op%22%3A%22LIKE%22%2C%22v%22%3A%5B%22%25%22%5D%7D%5D",
      "budget": { "achievableMs": 6500, "aspirationalMs": 1000 },
      "note": "P2 — terfisiz attribute filtresi (dizi yolu, GENİŞ eşleşme: user_agent.original LIKE %) → ham GROUP BY tam pencere; perfcheck taban p50 4.19 s (p95 5.6 s); ulaşılabilir = 1.5×; arzu 1 s yenilik dilimi / terfi+skip index ister"
    },
    {
      "name": "GET /api/traces", "scenario": "extras-50-2cols", "window": "1h", "cold": true,
      "path": "/api/traces?traceIds={traceIds}&extraAttrs=channel_code,function_code&from={from}&to={to}",
      "budget": { "achievableMs": 600, "aspirationalMs": 300 },
      "note": "P3 — attribute kolonları sayfadaki 50 id'ye sınırlı (idx_trace); perfcheck taban p50 377 ms"
    },
    {
      "name": "GET /api/servicegraph", "scenario": "focus-2hop-top500", "window": "1h", "cold": true,
      "path": "/api/servicegraph?focus={service}&hops=2&topN=500&from={from}&to={to}",
      "budget": { "achievableMs": 3500, "aspirationalMs": 500 },
      "note": "P4 — hop başına ardışık FINAL okuması; perfcheck taban p50 2.30 s. İstemci yerleşimi ayrı: frontend TopologyFlowGraph.perf.test.tsx"
    },
    {
      "name": "POST /api/dashboards/data", "scenario": "preset-dependencies-1h", "window": "1h", "cold": false,
      "method": "POST", "path": "/api/dashboards/data", "bodyFromDashboard": "preset-dependencies",
      "budget": { "achievableMs": 3000, "aspirationalMs": 1000, "maxBytes": 2000000 },
      "note": "P5 — v0.10.147: top-N 16 + kuyruk ön-toplamı → gövde 5.2 MB → 1.31 MB (122 seri); v0.10.146 panel-başı 30 s SWR cache; probe step 15 (FE ile aynı); v0.10.186 sütunsal nokta kodlaması (gövde enc:col opt-in, probe v0.10.188'den beri FE ile aynı gövdeyi gönderir — ölçüm perf/out/p5-188.json)"
    },
    {
      "name": "GET /api/problems", "scenario": "cold", "cold": true,
      "path": "/api/problems",
      "budget": { "achievableMs": 2500, "aspirationalMs": 200 },
      "note": "P6 — KAPANDI v0.10.144: FINAL 13–100 ms (7k satır); gecikme lokal CH CPU kuyruğu (query_log OSCPUWait 1.4–4.4 s vs CPU 60–290 ms), FINAL'sız okuma daha yavaş ölçüldü → kod değişikliği yok; perfcheck taban p50 0.78–1.54 s oynak; sıcak yol 15 s cache"
    }
  ]
}
